apiVersion: v2
name: observability-stack
description: OpenTelemetry pod log collection for Kubernetes through the API server — no DaemonSet, no host filesystem access; RBAC-scoped in cluster or namespace mode
type: application
version: 0.1.0
appVersion: "0.156.0"
keywords:
  - observability
  - opentelemetry
  - logging
  - multi-tenant
  - security
