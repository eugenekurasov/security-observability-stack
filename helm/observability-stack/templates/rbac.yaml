{{- /* RBAC is global (one grant per release), covering the union of signals
       owned across every collectorGroup. selfMonitoring (k8sattributes) and
       metrics.cluster (k8sclusterreceiver) gate off the global signal values. */ -}}
{{- $owned := include "observability-stack.ownedSignals" . | fromYamlArray -}}
{{- $selfMon := .Values.signals.selfMonitoring.enabled -}}
{{- $clusterMetrics := and (has "metrics" $owned) .Values.signals.metrics.cluster -}}
{{- if eq .Values.mode "cluster" }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "observability-stack.fullname" . }}
  labels:
    {{- include "observability-stack.labels" . | nindent 4 }}
rules:
  {{- if has "logs" $owned }}
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  {{- end }}
  {{- if has "metrics" $owned }}
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["nodes", "nodes/metrics", "nodes/proxy"]
    verbs: ["get", "list", "watch"]
  - nonResourceURLs: ["/metrics", "/metrics/cadvisor"]
    verbs: ["get"]
  {{- end }}
  {{- if has "events" $owned }}
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch"]
  {{- end }}
  {{- if $clusterMetrics }}
  # k8sclusterreceiver — cluster mode gets node/namespace access too
  - apiGroups: [""]
    resources: ["nodes", "namespaces", "resourcequotas", "replicationcontrollers"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "daemonsets", "statefulsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods", "nodes"]
    verbs: ["get", "list"]
  {{- end }}
  {{- if $selfMon }}
  # k8sattributes processor — resolve pod → owning deployment for enrichment
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["get", "list", "watch"]
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "observability-stack.fullname" . }}
  labels:
    {{- include "observability-stack.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "observability-stack.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: {{ include "observability-stack.fullname" . }}
  apiGroup: rbac.authorization.k8s.io

{{- else }}

{{- /* Namespace mode: one Role + RoleBinding per target namespace.
       Defaults to the release namespace so a tenant can self-install
       by simply running `helm install` in their own namespace. */}}
{{- $root := . }}
{{- $targetNs := default (list .Release.Namespace) .Values.namespaces }}
{{- range $ns := $targetNs }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "observability-stack.fullname" $root }}
  namespace: {{ $ns }}
  labels:
    {{- include "observability-stack.labels" $root | nindent 4 }}
rules:
  {{- if has "logs" $owned }}
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  {{- end }}
  {{- if has "metrics" $owned }}
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints"]
    verbs: ["get", "list", "watch"]
  {{- end }}
  {{- if has "events" $owned }}
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch"]
  {{- end }}
  {{- if $clusterMetrics }}
  # k8sclusterreceiver — namespace mode excludes nodes/namespaces (cluster-scoped)
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "daemonsets", "statefulsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods"]
    verbs: ["get", "list"]
  {{- end }}
  {{- if $selfMon }}
  # k8sattributes processor — resolve pod → owning deployment for enrichment
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["get", "list", "watch"]
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "observability-stack.fullname" $root }}
  namespace: {{ $ns }}
  labels:
    {{- include "observability-stack.labels" $root | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "observability-stack.serviceAccountName" $root }}
    namespace: {{ $root.Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "observability-stack.fullname" $root }}
  apiGroup: rbac.authorization.k8s.io
{{- end }}

{{- end }}
